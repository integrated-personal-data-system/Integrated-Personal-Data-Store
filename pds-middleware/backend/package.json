{
  "name": "server",
  "version": "1.0.0",
  "main": "server.ts",
  "scripts": {
    "start": "(cd ../frontend && npm run build) && (cd ../backend && sudo nodemon server.ts)",
    "docker-start": "(cd ../frontend && npm run build) && (cd ../backend && nodemon server.ts)"
  },
  "license": "MIT",
  "dependencies": {
    "@trinsic/service-clients": "^1.1.5018",
    "@types/morgan": "^1.9.3",
    "@types/node": "^17.0.40",
    "api": "^4.5.1",
    "body-parser": "^1.20.0",
    "dotenv": "^16.0.1",
    "express": "4.17.1",
    "express-winston": "^4.2.0",
    "fs": "^0.0.1-security",
    "node-fetch": "2",
    "node-rsa": "^1.1.1",
    "nodemon": "2.0.3",
    "path": "^0.12.7",
    "react-bootstrap-validation": "^0.1.11",
    "sparqljs": "^3.5.2",
    "ts-node": "^10.8.2",
    "uuid": "^8.3.2",
    "winston": "^3.8.1"
  }
}
