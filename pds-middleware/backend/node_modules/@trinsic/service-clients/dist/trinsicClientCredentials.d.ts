import { ServiceClientCredentials, WebResource } from "@azure/ms-rest-js";
import { CredentialsServiceClientOptions } from "./credentials/models";
import { WalletServiceClientOptions } from "./wallet/models";
import { ProviderServiceClientOptions } from "./provider/models";
export declare class TrinsicCredentialsClientOptions implements CredentialsServiceClientOptions {
    noRetryPolicy?: boolean;
    baseUri?: string;
}
export declare class TrinsicWalletClientOptions implements WalletServiceClientOptions {
    noRetryPolicy?: boolean;
    baseUri?: string;
}
export declare class TrinsicProviderClientOptions implements ProviderServiceClientOptions {
    noRetryPolicy?: boolean;
    baseUri?: string;
}
export declare class TrinsicClientCredentials implements ServiceClientCredentials {
    accessToken: string;
    constructor(accessToken: string);
    signRequest(webResource: WebResource): Promise<WebResource>;
}
export declare class TrinsicProviderCredentials implements ServiceClientCredentials {
    providerKey: string;
    constructor(providerKey: string);
    signRequest(webResource: WebResource): Promise<WebResource>;
}
